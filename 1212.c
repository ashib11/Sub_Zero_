#include<stdio.h>
#include<string.h>
int main()
{
int a,b; 
while (1)
{
        scanf("%d %d", &a,&b); 
        if (a == 0 && b == 0)
        {
            break; 
        }
        while (a!=0)
        {
            /* code */
        }
        
}

return 0;
}